package constructit

/**
 * Browser entry point (placeholder — the interactive shell is built in Phase C).
 * For now it just confirms the Kotlin/JS target compiles and links the shared engine.
 */
fun main() {
    println("ConstructIt browser shell — placeholder")
}
